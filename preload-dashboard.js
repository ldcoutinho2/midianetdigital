const fs=require('fs'),path=require('path'),axios=require('axios');const app=require('express/lib/application');const og=app.get,op=app.post;const ui=fs.readFileSync(path.join(__dirname,'dashboard-admin.html'),'utf8');function ok(req){return req.query.senha===process.env.DASHBOARD_PASSWORD||req.headers['x-dashboard-password']===process.env.DASHBOARD_PASSWORD}app.get=function(p,...h){if(p==='/dashboard')return og.call(this,p,(req,res)=>{if(req.query.usuario!=='admin'||!ok(req))return res.status(401).send('Não autorizado');res.send(ui)});if(p==='/admin/site-config')return og.call(this,p,async(req,res)=>{if(!ok(req))return res.status(401).json({error:'Não autorizado'});try{if(!process.env.SUPABASE_URL||!process.env.SUPABASE_KEY)return res.json({});const u=process.env.SUPABASE_URL+'/rest/v1/eventos?select=*&tipo=eq.config_site&order=created_at.desc&limit=1';const r=await axios.get(u,{headers:{apikey:process.env.SUPABASE_KEY,Authorization:'Bearer '+process.env.SUPABASE_KEY}});res.json(r.data&&r.data[0]?JSON.parse(r.data[0].nome||'{}'):{});}catch(e){res.status(500).json({error:'Erro ao carregar site'})}});return og.call(this,p,...h)};app.post=function(p,...h){if(p==='/admin/site-config')return op.call(this,p,async(req,res)=>{if(!ok(req))return res.status(401).json({error:'Não autorizado'});try{if(!process.env.SUPABASE_URL||!process.env.SUPABASE_KEY)return res.status(500).json({ok:false,error:'Supabase não configurado'});const b={nome:String(req.body?.nome||''),whatsapp:String(req.body?.whatsapp||''),horario:String(req.body?.horario||''),garantia:String(req.body?.garantia||''),texto:String(req.body?.texto||'')};await axios.post(process.env.SUPABASE_URL+'/rest/v1/eventos',{tipo:'config_site',nome:JSON.stringify(b),valor:0},{headers:{apikey:process.env.SUPABASE_KEY,Authorization:'Bearer '+process.env.SUPABASE_KEY,'Content-Type':'application/json',Prefer:'return=minimal'}});res.json({ok:true,item:b})}catch(e){res.status(500).json({ok:false,error:e.message||'Erro'})}});return op.call(this,p,...h)};